import Button from '@components/Button'
import Input from '@components/Input'
import Switch from '@components/Switch'
import Protected from '@components/Protected'

export { Button, Input, Switch, Protected }
